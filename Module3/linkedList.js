class Node {
    constructor(value) {
        this.value = value;
        this.next = null;
    }
}

// const head = new Node(10)

// head.next = new Node(20)

// head.next.next = new Node(30)

// let temp = head;
// console.log(temp)
// while (temp !== null) {
//     console.log(temp.value, " ");
//     temp = temp.next;
// }


class LinkedList {
    constructor() {
        this.head = null;
        this.tail = null;
        this.length = 0;
    }

    append() { }

    prepend() { }

    insert() { }

    remove() { }

    print() { }
}